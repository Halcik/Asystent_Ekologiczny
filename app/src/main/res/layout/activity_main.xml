<?xml version="1.0" encoding="utf-8"?>
<!-- Główny layout aplikacji: toolbar + kontener fragmentów + dolna nawigacja -->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/solid_blue"
    tools:context=".MainActivity">

    <!-- Pasek aplikacji (MaterialToolbar). Tytuł nadpisywany dynamicznie w MainActivity -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary"
        android:theme="@style/ThemeOverlay.MaterialComponents.ActionBar"
        android:title="Asystent Ekologiczny"
        app:titleTextColor="@color/toolbar_title"
        app:titleMarginTop="25dp" />

    <!-- Kontener na fragmenty (margines top = wysokość toolbara, bottom = miejsce dla BottomNavigation) -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="56dp"
        android:layout_marginTop="?attr/actionBarSize" />

    <!-- Dolna nawigacja między głównymi sekcjami -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconTint="@color/bottom_nav_item_colors"
        app:itemTextColor="@color/bottom_nav_item_colors" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
